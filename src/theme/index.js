export { colors, getColor } from '../theme/colors';
export { defaultTheme, defaultThemeConstants, createTheme, isDefaultTheme } from '../theme';
export { ThemeProvider } from '../theme/theme-context';
